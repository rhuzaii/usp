Write a program to demonstrate the zombie state of a process and
provide the solution for the same.

#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>

int main() {
    pid_t pid = fork();

    if (pid < 0) {
        perror("fork failed");
        return 1;
    }

    if (pid == 0) {
        // Child process
        printf("Child (PID: %d) exiting\n", getpid());
        exit(0);
    } else {
        // Parent process
        printf("Parent (PID: %d) sleeping\n", getpid());
        sleep(20);   // 👈 child becomes zombie during this time
    }

    return 0;
}


solution 
nano q12as.c
#include <stdio.h>
#include <unistd.h>
#include <sys/wait.h>
#include <stdlib.h>

int main() {
    pid_t pid = fork();

    if (pid == 0) {
        printf("Child exiting\n");
        exit(0);
    } else {
        wait(NULL);   // ✅ collect child status
        printf("Parent collected child\n");
    }

    return 0;
}


ps aux | grep zombie
ps -el | grep Z


ranaadiba@Ranas-MacBook-Air usp % nano q12a.c
ranaadiba@Ranas-MacBook-Air usp % gcc q12a.c -o q12a
ranaadiba@Ranas-MacBook-Air usp % ./q12a
Parent (PID: 20049) sleeping
Child (PID: 20050) exiting
ranaadiba@Ranas-MacBook-Air usp % nano q12as.c
ranaadiba@Ranas-MacBook-Air usp % gcc q12as.c -o q12as
ranaadiba@Ranas-MacBook-Air usp % ./q12as
Child exiting
Parent collected child
