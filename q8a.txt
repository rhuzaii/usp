Write a C program that takes the file name as an argument and prints the type of the given file


#include <stdio.h>
#include <sys/stat.h>
#include <unistd.h>

int main(int argc, char *argv[]) {
    struct stat st;

    if (argc < 2) {
        printf("Usage: %s <filename>\n", argv[0]);
        return 1;
    }

    // Use lstat instead of stat
    if (lstat(argv[1], &st) == -1) {
        perror("lstat failed");
        return 1;
    }

    if (S_ISREG(st.st_mode))
        printf("Regular file\n");
    else if (S_ISDIR(st.st_mode))
        printf("Directory\n");
    else if (S_ISLNK(st.st_mode))
        printf("Symbolic link\n");
    else if (S_ISCHR(st.st_mode))
        printf("Character device\n");
    else if (S_ISBLK(st.st_mode))
        printf("Block device\n");
    else if (S_ISFIFO(st.st_mode))
        printf("FIFO (Pipe)\n");
    else if (S_ISSOCK(st.st_mode))
        printf("Socket\n");
    else
        printf("Unknown file type\n");

    return 0;
}


nano q8a.c
gcc q8a.c -o q8a 
./q8a file.txt 
output: regular file 
./q8a testdir 
output: directory 


ranaadiba@Ranas-MacBook-Air usp % nano q8a.c
ranaadiba@Ranas-MacBook-Air usp % gcc q8a.c -o q8a       
ranaadiba@Ranas-MacBook-Air usp % ./q8a file.txt     
Regular file
ranaadiba@Ranas-MacBook-Air usp % ./q8a testdir   
Directory
ranaadiba@Ranas-MacBook-Air usp % ln -s file.txt link.txt
ln: link.txt: File exists
ranaadiba@Ranas-MacBook-Air usp % ./q8a link.txt         
Symbolic link
